<meta charset="utf-8" />
     <meta http-equiv="X-UA-Compatible" content="IE=edge">
     <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Titre</title>
    </head>

    <body>
    <table> 
    <tr>
    <th> id </th>
    <th> userid </th>
    
    </tr>
    {% for p in panier %}
    <tr>
    <td>{{p.id}} </td>
    <td>{{p.userid}}</td>
   <td><a href="{{ path("SuppP",{"id":p.id}) }}">supprimer</a></td>
    <td> <a href="{{ path('ModifP',{'id':p.id}) }}">Modifier</a></td>
    
    </tr>
    {% endfor %}
    
    </table>
    <a href="{{ path('AjoutP') }}">Ajouter</a>
    </body>
</html> 