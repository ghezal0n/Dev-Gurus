{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ user.pseudo }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ user.mdp }}</td>
            </tr>
            <tr>
                <th>Points</th>
                <td>{{ user.points }}</td>
            </tr>
            <tr>
                <th>AddressLoc</th>
                <td>{{ user.addressLoc }}</td>
            </tr>
            <tr>
                <th>NumTelf</th>
                <td>{{ user.numTelf }}</td>
            </tr>
            <tr>
                <th>Level</th>
                <td>{{ user.level }}</td>
            </tr>
            <tr>
                <th>Cv</th>
                <td>{{ user.Cv }}</td>
            </tr>
            <tr>
                <th>Badge</th>
                <td>{{ user.badge }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
