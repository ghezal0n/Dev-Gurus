{% extends 'backend/dashboard_c/dashboard.html.twig' %}


{% block title %}Visual Admin Dashboard - Product{% endblock %}
   
    
{% block content %}

    <div class="templatemo-content-container">
      <div class="templatemo-content-widget no-padding">
        <div class="panel panel-default table-responsive">
          <table class="table table-striped table-bordered templatemo-user-table">
            <thead>
              <tr>
                <td><a href="" class="white-text templatemo-sort-by">ID <span class="caret"></span></a></td>
                <td><a href="" class="white-text templatemo-sort-by">Nom du jeux <span class="caret"></span></a></td>
                <td><a href="" class="white-text templatemo-sort-by">Image<span class="caret"></span></a></td>
                <td>Edit</td>
                <td>Delete</td>
              </tr>
            </thead>
            <tbody>
              <tr{% for j in jeux%}>

                <td>{{j.id}}</td>

                <td>{{j.getNomJeux}}</td>

                <td>{{j.getImageJeu}}</td>

                <td><a href="{{path('Update',{'id':j.id})}}" class="templatemo-edit-btn">Edit</a></td>
                <td><a href="{{path('d',{'id':j.id})}}" class="templatemo-link">Delete</a></td>
              </tr {% endfor %}>
            </tbody>
          </table>    
        </div>

        <a href="{{ path('testing') }}"><button class="templatemo-blue-button">Ajouter Jeu</button></a>
      </div>
         
      <footer class="text-right">
        <p>Copyright &copy; 2084 Company Name 
        | Design: Template Mo</p>
      </footer>         
    </div>
  </div>
</div>

<script>
  $(document).ready(function(){
    // Content widget with background image
    var imageUrl = $('img.content-bg-img').attr('src');
    $('.templatemo-content-img-bg').css('background-image', 'url(' + imageUrl + ')');
    $('img.content-bg-img').hide();        
  });
</script>
</body>
{% endblock %}
